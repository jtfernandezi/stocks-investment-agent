# Improvement Backlog — Initiatives Register

Living, ranked list of ideas for making the fund better, plus the **Lever 7b R&D research agenda** (below). Maintained by the weekly audit (`automation/weekly_audit_prompt.md`) — it adds evidence-grounded ideas, re-ranks each week, dedups, advances maturity grades, and tracks whether applied ideas actually worked.

**The audit now CODES some of these into PRs** (reworked 2026-06-11). Each item maps to a tier:
- **`[A]`** tunable fix · **`[B-code]`** isolated, code-only, low-blast-radius change (no n8n wiring) → both get **coded into PRs** (human merges).
- **`[B-spec]`** needs n8n wiring, or is a strategy/judgment call, or isn't verifiable yet → **spec only**, built deliberately by a human. Strategies stay here until a backtest harness exists.

**Status legend:** `proposed` → `coded PR#<n>` → `merged` → `worked` / `didn't` / `dropped`
**Maturity (R&D agenda):** `speculative` (plausible, no evidence) → `promising` (some supporting evidence) → `evidence-backed` (data clearly supports it; if isolated/code-only, eligible to become a `[B-code]` PR)

## Structural backlog (integrity / tunable / guardrail)

| Rank | Idea | Tier | Evidence (what data motivated it) | Impact | Effort | Risk | Status | First raised |
|------|------|------|-----------------------------------|--------|--------|------|--------|--------------|
| 1 | **Rewire `Store Open Trade`/`Store Position Entry` to post-execution branch** (prevention half) — Currently wired from `Process Post-Trade` (parallel to trade execution), so all orchestrator-proposed BUY/SHORTs are written to DB even if `08_prepare_trade_actions.js` blocks them at execution time. Produces phantom OPEN trades for every session where any hard limit fires. **Detection half coded 2026-06-21** (canary reconciliation, structural #10 / PR this week). | `[B-spec]` | SMR+SNPS orphaned as OPEN (2026-06-08) — never executed. **2026-06-11:** hand-cleaned, wiring unchanged. **2026-06-12:** wiring re-confirmed live. PR #3 (`[A]`) filters them from the trade-lessons feed. **2026-06-14:** wiring STILL live; not exercised (zero BUYs). **2026-06-21 — EXERCISED LIVE:** the 06-18 SLB short (2nd oil_gas short) was blocked by `08` but `Store Open Trade` wrote it anyway → SLB OPEN in `trades`+`position_metadata`, absent from Alpaca → DB↔Alpaca desync (5 vs 4). Contained (orchestrator view sourced from Alpaca; false-PM guarded by `if(!tsOrder) return []`) but latent hazard: a future real SLB trade's INSERT silently fails (`trades_one_open_per_ticker`). **2026-06-24 — RESOLVED (both halves).** Recurred (RTX/SLB/HBM/BLK; 14 vs 10) and turned out to be *two* bugs. Prevention shipped a simpler way than the proposed rewire: `Prepare Position Metadata` now filters its BUY/SHORT list to the tickers that passed `08` (`$("Prepare Trade Actions").all()`) — blocked actions write no row, no n8n rewire. Detection merged as **PR #12** (canary reconciles `position_metadata` too). Also fixed the sibling *missed-close* bug (RTX/HBM): `Trigger Post-Mortem` was once-for-all → only the first SELL in a multi-SELL session got a post-mortem; now `mode: each`. See CLAUDE.md Enhancements 2026-06-24. **2026-06-28:** verified DB↔Alpaca back to 7=7; SLB/BLK/SMR/SNPS all `CLOSED never_executed`. | High | Medium | Low | **`merged` (2026-06-24 — prevention via filter + detection via PR #12)** | 2026-06-10 |
| 12 | **Trailing-stop coverage guarantee** — every live Alpaca long/short must carry an open `trailing_stop` order. **Detection** = canary check (fetch positions + open orders, alarm on any position with no covering trailing_stop). **Prevention** = lengthen `Wait For Fill` and/or a Watchdog reconciliation that submits a missing stop at the standard ATR×3 / [8,20] band. | detection `[B-code]` · prevention **BUILT 2026-07-24** | 2026-06-28: the 06-22 09:32 morning batch of 4 longs (C, CEG, HBM, VRT) opened with NO trailing stops — `Wait For Fill` (5s) < actual fill latency (VRT filled 6m28s after submission), so `Submit Trailing Stop` fired before the buy filled → Alpaca rejected the stops. The midday batches (faster fills) got stops fine. Canary had no stop-coverage check → silent. Same "silent failure the canary can't see" family as #1/#10. **2026-07-03 — detection VALIDATED LIVE:** PR #15 merged 06-28 → operator remediated per spec (C closed 06-29, VRT stop manually attached 06-30 at 8% GTC) → **that stop fired correctly 07-01** (VRT closed −6.64%, inside band, clean post-mortem). Full detect→remediate→protect loop worked. Coverage now 2/2, canary green daily. Prevention half still unbuilt — urgency re-couples to deployment resuming (PR #18): the next multi-order batch re-exposes the 5s `Wait For Fill`. **2026-07-13 — RECURRED, 3rd occurrence:** NOC/NXPI (07-09 09:32 batch, fills 43s/85s) and PLTR (07-10 09:32, fill 2m41s) opened unprotected; canary alarmed daily; operator attached 11% GTC stops manually 07-13 (coverage 8/8 verified). Live `Wait For Fill` node found with **empty parameters = 1s n8n default**, not the documented 5s. Fill-time evidence says a 60s bump saves only ~1 of 3; the Watchdog reconciliation branch is the required fix. **2026-07-24 — RECURRED 4th time (DDOG/PANW/SNOW 07-22/23, FCX/HAL 07-24, fills 41-98s) — PREVENTION FINALLY BUILT.** `Wait For Fill` bumped 1s→120s (partial mitigation, still not sufficient for a true multi-minute outlier like PLTR's 161s on 07-10). The real fix: new self-healing Watchdog branch (8 nodes — `Fetch Alpaca Open Orders (Self-Heal)` → `Find Missing Stops` → `IF Has Missing Stops?` → `Split Missing Stops`/`Build Self-Heal Email` → `Submit Missing Stop` (12% flat GTC fallback, not ATR-based — Watchdog has no bar data) + `Send Self-Heal Alert` Gmail) wired live into the Watchdog workflow, checking every 30 min. Two new code files: `watchdog_find_missing_stops.js`, `watchdog_build_selfheal_email.js` (in `n8n_manifest.json`, auto-synced going forward). First real-world exercise is the next scheduled Watchdog tick after 2026-07-24 16:10 UTC — expected to find 0 missing (all 10 live positions were manually protected same session) and stay silent; watch for its first actual catch. **2026-08-02 — STILL UNEXERCISED, and now un-exercisable:** the Watchdog has been `active=false` since 2026-07-27 (structural #19), so the self-heal branch cannot run at all. Coverage is nonetheless **8/8** today — but 6 of the 8 stops were attached by the operator by hand on 07-24; only NXE's (07-27 14:14, 10% GTC) was submitted by the pipeline, which does confirm the 120s `Wait For Fill` working on a live entry. The stops were also the *only* thing protecting the book through the 4-day outage, since they execute broker-side. First genuine test of the self-heal branch is still ahead. | High | Low (detect) / Med (prevent) | Low | **detection `merged PR#15, worked (2026-07-01)`; prevention `built 2026-07-24`, unexercised (Watchdog off since 07-27)** | 2026-06-28 |
| 13 | **Inception-baseline reporting integrity** — `02` computed portfolioCumulativePct / spyCumulativePct / the orchestrator's Alpha line against the oldest row of the `Load Portfolio Snapshots` LIMIT-30 window, i.e. a rolling ~6-trading-day baseline, not inception. Mislabeled rolling alpha fed to every orchestrator session since ~06-05 (snapshot #31), and stored garbage in `portfolio_snapshots.spy_cumulative_pct` (07-01: +1.39% → +2.33% → +1.51% intraday; true inception ≈ −0.5%). Fix: hardcode the verified inception anchors ($60,000 / SPY $750.46, 2026-05-28 reset). | `[A]` | 2026-07-03: found while reconciling snapshot `spy_cumulative_pct` against the audit's own inception math; confirmed via `Load Portfolio Snapshots` node SQL (GET) + first-snapshot row. The orchestrator reasoned about its beat-SPY mandate with a wrong scoreboard for ~4 weeks, spanning the entire cash-drag saga. | Medium | Low | Low | **`coded PR#17` (2026-07-03)** | 2026-07-03 |
| 2 | **Encourage orchestrator to deploy into uncorrelated niches when 8+ niches BULLISH** — System prompt could explicitly note that deploying into low-correlation niches (healthcare, financials) is preferred over holding cash when high-conviction signals are sustained across most sectors. | `[B-spec]` | 74% cash with 8/10 niches BULLISH. **2026-06-11:** orchestrator IS watchlisting these (LLY/CEG/VST triggers) — disciplined-waiting, not blind. **2026-06-12 — ESCALATED to evidence-backed:** first week the cash drag *measurably* cost relative performance (fund captured only ~56% of SPY's +1.78pp rally; alpha narrowed +2.05%→+1.26%). Root contributor diagnosed: the EV-override fires on a tiny, phantom-polluted, regime-stale cold-start sample (see R&D top bet + spec #2). Cash 67.9%. | Medium→High | Low | Low | `proposed` | 2026-06-10 |
| 3 | **Earnings calendar deduplication + universe prune** — Add `ON CONFLICT (ticker, earnings_date) DO NOTHING` to `Store Earnings Calendar` SQL + UNIQUE constraint `(ticker, earnings_date)`; optionally prune rows for tickers no longer in the universe. | `[B-spec]` | ORCL dup (Jun 10 + Jun 16). **2026-06-12: still dup, plus MU and UEC now also dup; MU is a *stale* ticker (removed from universe 2026-05-28) with 2 leftover rows never pruned.** All harmless while unheld. | Low | Low | Low | `proposed` | 2026-06-10 |
| 4 | **Execution audit log table** — New table `execution_log` (session_id, ticker, proposed_action, executed bool, block_reason text), written by `08_prepare_trade_actions.js` for every proposed trade. Makes cap/limit firings visible in future audits instead of discoverable only by cross-referencing DB vs Alpaca orders. | `[B-spec]` | SMR/SNPS phantom trade was only caught by manually diffing `trades` against Alpaca GET /v2/orders — fully invisible in normal monitoring. Complements #1: even after the rewire, this surfaces *why* a trade was blocked. | Medium | Medium | Low | `proposed` | 2026-06-10 |
| 5 | **Harden correlation penalty from advisory to soft size-cap** — The 0.70 correlation rule is currently a prompt-level penalty the orchestrator can override. Consider a soft size reduction (or a 3rd-correlated-long block) enforced in `08`. | `[B-spec]` | 2026-06-11: open longs DDOG–FTNT 0.78 and FTNT–PANW 0.76 both exceed 0.70; three correlated tech longs held simultaneously. Within tolerance now, but a concentration risk if cash deploys into more tech. | Medium | Medium | Low | `proposed` | 2026-06-11 |
| 6 | **Expand position cap to 14 when 10 niches active** ⛔ outside operating envelope (requires prompt + code change; needs 20+ closed trades for calibration before tuning) | `[B-spec]` ⛔ | 12 positions across 10 niches = 1.2 trades/niche; limits sector pair coverage when all niches BULLISH. | Medium | High | Medium | `proposed` | 2026-06-10 |
| 7 | **Exclude `never_executed` phantoms from the orchestrator trade-lessons feed** — filter `exit_reason='never_executed'`/`outcome IS NULL` from `recentTradeLessons` in `02` before `.slice(0,5)`. | `[A]` | 2026-06-12: SMR/SNPS phantoms (cleaned to CLOSED ~06-11, recent `updated_at`) sat atop the last-5 window with `outcome=NULL`; `formatTradeStats` counts non-WIN as loss → fake `nuclear 0/1`/`semis 0%` fed to orchestrator, which declined CEG/VST + semis. Complements #1 (#1 stops the write; #7 stops the corruption). **2026-06-14: MERGED + LIVE in n8n** (`Compute Derived Metrics` jsCode verified to contain the filter). Effect not yet measurable — zero new BUYs since merge; re-judge on first new entry. | Medium | Low | Low | **`merged` (live, effect pending)** | 2026-06-12 |
| 8 | **Minimum-sample floor on the orchestrator EV-override rule** — 06 prompt §4d (line ~505) says "a pattern at 0% win rate in your closed trades → hard negative EV, overrides historical prior," with NO sample floor (and contradicts line ~506). Fires on a tiny cold-start sample. Add a floor (require ≥4 closed trades in that pattern/niche before live overrides prior; never refuse a HIGH-conviction signal on a <4-trade record). | `[B-spec]` | 2026-06-12: contributor to 68% cash drag. **2026-06-17 — ESCALATED to ACTIVE DOMINANT CAUSE:** alpha collapsed +1.29%→~−1.70% in one week (SPY +3pp, fund flat) because the orchestrator refused 7 HIGH-conviction BULLISH signals AND closed its own TREND winners every session, citing TREND EV −3.49% on a **2-trade** sample. The §7b annotation half is coded (item #9 / PR #7); this §4d behavioral half is the larger lever, stays `[B-spec]` (changes deployment judgment, unverifiable without backtest). Scaffold in 2026-06-17 report. | **High** | Low | Medium | `proposed` (active dominant lever) | 2026-06-12 |
| 9 | **Honor the documented ≥5-trade floor in pattern-EV annotations** — `formatPatternPerf()` in `06` rendered `← NEGATIVE EV — DO NOT TRADE` / `← LOW EV` flags with no sample gate, contradicting the prompt's own §7b rule (apply EV only with ≥5 trades). Gate the flags behind `total_trades >= 5`; below the floor render `← insufficient sample (n<5) — EV unreliable, ignore per §7b`. | `[A]` | 2026-06-17: TREND row = 2 trades, 0 wins, EV −3.49% → flagged "DO NOT TRADE"; orchestrator cited it to freeze ~74% of the book in cash through a +3pp SPY rally. **2026-06-21: MERGED + live** (verified `06` line 151 `trades>=5`). Effect: numeric EV citation gone; SLB TREND short proposed (no longer mechanically refused). BUT cash-drag behavior re-routed to *qualitative* "extended TREND = no edge" prose — necessary-but-not-sufficient (see top R&D bet). | Medium | Low | Low | **`merged` (worked at its layer; behavior re-routed)** | 2026-06-17 |
| 8b | **Minimum-sample floor on §4d "live record overrides prior" rule** (the §4d behavioral half of #8). | `[A]` | 2026-06-17: coded by a concurrent run as PR #8 (entry gate, not exit). **2026-06-21: MERGED + live** (verified `06` line 518, ≥5-closed-trade floor). Same outcome as #9: numeric override channel closed; deployment behavior persists qualitatively. | High | Low | Med | **`merged` (worked at its layer; behavior re-routed)** | 2026-06-17 |
| 10 | **DB↔Alpaca open-position reconciliation in the daily canary** — fetch live Alpaca positions (GET) and compare the symbol set to `trades WHERE status='OPEN'`; alarm on phantom OPEN rows (in DB, not Alpaca) or untracked live positions (in Alpaca, not DB). Detection half of #1. | `[B-code]` | 2026-06-21: the SLB phantom created a 5-vs-4 DB↔Alpaca desync that the canary completely missed — it has no reconciliation check. Monitoring-layer only (read-only + Alpaca GET), zero fund blast radius, deterministic. Would have alarmed the moment the phantom was written. **2026-06-24: MERGED as PR #12** — and extended to also reconcile `position_metadata` vs Alpaca (caught 5 stale rows AMAT/DDOG/FTNT/GD/LHX the trades-only check missed). | High | Low | Low | **`merged` PR#12 (2026-06-24)** | 2026-06-21 |
| 11 | **Orchestrator summary↔action integrity** — the `orchestrator_summary` free-text repeatedly described exits it never emitted (06-17, 06-18). | `[B-spec]` | 2026-06-21: 2 data points. **2026-06-28 — NO NEW DIVERGENCE:** 06-26 summaries accurately describe the exact 7-position held book; the GD/RTX exits narrated 06-18 actually executed 06-22. Watch only; demote unless it recurs. | Medium | Low-Med | Low | `proposed` (dormant — no recurrence 06-28) | 2026-06-21 |
| 14 | **Phase 1 entry/exit discipline overhaul** (operator-built, interactive session) — five structural fixes from closed-trade forensics (22 valid trades): (1) hard entry-extension gate in `08` (no BUY >+5% above 20d SMA / SHORT >-5% below — losing longs averaged +5% 5-day run-ups at entry, losing shorts entered 12-16% below the mean); (2) entry throttle (max 2 new entries + $12k/session — 23/25 entries had landed on 3 batch days); (3) exit symmetry (5-day min hold before judgment exits, thesis_flip needs 2+ sessions AND 20d-MA break — thesis_flip was 0/6, profit_taking banked +$21/8 trades); (4) price-based entry taxonomy (see R&D monoculture row); (5) computed market regime (see R&D regime row). | `[A]`/`[B-code]` mix | 2026-07-03 full-stack expert review + per-trade entry-extension analysis vs Alpaca bars. All thresholds UNVERIFIED (no backtest). Watch items: deployment resumes via PULLBACK entries; thesis_flip stops selling bottoms; `[LIMIT]` gate logs. **Note: throttle means rebuilding to the BULL band takes ~3-5 trading days — slow deployment next week is the design, not the deadlock.** **2026-07-05: deployed (merge-sync success, 20/20 in sync, inline nodes verified via GET) but ZERO live sessions yet — 07-03 was the observed holiday; first contact Mon 07-06 09:30.** First harness replay (PR #22): **short-side gate VALIDATED** (shorting >5% below the 20d SMA = worst bucket, −5.89% avg fwd 21d); **long-side gate contradicted-in-period** (extended longs best bucket +6.39% avg, EXCEPT in BEAR regime −1.10%) → see new regime-conditioned-gate R&D row. **2026-07-19 — FIRST LIVE FORTNIGHT (07-06→07-17): entry side worked** (throttle respected 100% of sessions, PULLBACK/MOMENTUM entries, ranked overflow watchlisted, no prose-veto, losers cut small — Phase-1-era entries ≈ +$80 realized broker-true); **exit side routed around the min-hold rule via exit_reason vocabulary** (DELL 2.6h "give-back", WFC same-day "earnings_risk" on a date known at entry, AMAT "position_aging" @5d, SMCI profit-take @3d) → runbook spec #4; **taxonomy EV collection was dead on arrival** (structural #17) and entry labels for stale-bars tickers are garbage (structural #16). | High | Med | Med | **`merged` PR#20 — entry half `worked (2 wks)`, exit half `didn't (vocabulary loophole)`, feedback half `blocked (#17)`** | 2026-07-03 |
| 16 | **Bars `limit`-exhaustion staleness class** — Alpaca's multi-symbol bars endpoint applies `limit` as a TOTAL across the request, filling alphabetically; the live fetch nodes use a FIXED `start=2025-01-01`, so the bars-per-symbol requirement grows every trading day while the limit stays constant. Once exhausted, the alphabetically-last ticker per niche silently receives a truncated series ending months in the past. Detection = canary replay of the live requests (PR #25); guard = fail-closed stale-price entry block in `02`/`08` (PR #26); root fix = n8n `limit` bump 3600→10000 / 4400→10000 (or rolling `start`) — operator-manual, ~5 min. | detect/guard `[B-code]` · root fix `[B-spec]` | 2026-07-19: outgrown since ~06-06 — ZS/RTX/VST/VALE/TER/WDAY/XOM/WDC/UNH/WFC served 2025-07-18 prices for ~a month. Real corruption: VST recorded −$1,397 (real −$56), WFC recorded −$179 (real +$4), XOM open row entry $107.60 vs $143.13 filled ($6.6k of a $5k intent). Fortnight realized P&L: DB −$540 vs broker-true +$1,040. SPY/regime node (4400) exhausts ~early Aug. 3rd instance of the "fixed limit/schema vs growing data" class (watchlist CHECK 06-08, pattern CHECK #17). **2026-08-02 — RESOLVED.** Operator applied the runbook 2026-07-21: all 11 bars nodes now `limit=10000` (verified by GET). Canary replay finds **zero** stale series across all 100 tickers + 11 ETFs. Book corrections (VST/WFC/XOM/DELL) also applied and verified in `trades`. PR #25's mirror briefly alarmed on its own stale copy of the request (operator fixed 07-24, commit fc18eac) — a false positive, correctly diagnosed. Caveat carried forward: the DELL reconstruction INSERT omitted `entry_pattern`, which froze the pattern-EV loop for 2 weeks (new structural #20). | **High** | Low | Low | **`merged PR#25/#26`, root fix `worked (2026-07-21)`** | 2026-07-19 |
| 17 | **`pattern_performance` CHECK constraint blocks Phase-1 taxonomy** — `pattern_performance_pattern_type_check` allows only TREND/BIAS/NOISE/REVERSAL/FIRST_SIGNAL; every post-Phase-1 close carries PULLBACK/MOMENTUM/etc., so the `Update Pattern Performance` recompute has failed on every post-mortem since 07-10 (table frozen at 07-08 18:41). The new-taxonomy EV feedback loop — the point of Phase 1 commit 4 — has recorded nothing. Fix: 1-line ALTER adding the 6 new labels (DB migration → operator). Rest of the post-mortem chain unaffected (trades close, accuracy updates, metadata deletes → canary-invisible). | `[B-spec]` (DB migration) | 2026-07-19: `updated_at` frozen at DELL's 07-08 post-mortem despite 4 closes since; constraint dump confirms the legacy-only allowlist. Same class as the 2026-06-08 watchlist `direction` constraint. **2026-08-02 — CONSTRAINT FIXED, TABLE STILL FROZEN.** The ALTER was applied (verified via `pg_get_constraintdef` — all 6 Phase-1 labels now allowed) and a one-time recompute ran 2026-07-21 05:29. But the table has not updated since, for a *different* reason: a NULL `entry_pattern` now aborts the whole recompute (structural #20). Net effect on the Phase-1 EV feedback loop is unchanged — still zero new-taxonomy rows, 3 weeks on. | High | Low | Low | **`merged` (constraint fixed 2026-07-21); superseded by #20** | 2026-07-19 |
| 18 | **Orchestrator JSON-escape parse fragility** — GPT-5.1 occasionally emits invalid JSON escapes (`\$28k`); `07`'s strict parse then discards the entire session (2026-07-10 watchdog run → "could not be parsed", all actions dropped — a watchdog flip exists precisely to close a threatened position, so this drops exactly that close). The 07-12 audit coded a lenient-retry fix as PR #24; the operator closed it unmerged 07-14 (no recorded reason). Failure mode remains open; single occurrence so far. | `[A]` (operator-declined) | 2026-07-10: n8n exec 1540, char 15142 — that run happened to carry 0 actions, so nothing was lost this time. No recurrence 07-11→07-17. | Medium | Low | Low | **`dropped` (PR #24 closed by operator; re-raise only if it recurs)** | 2026-07-12 |
| 19 | **Pipeline liveness: the fund can be switched off and stay off** — all four `[Stocks]` n8n workflows went `active=false` after the 2026-07-27 close and stayed off for 4 trading days (07-28→07-31): no analysis, no watchdog, no fundamentals, no snapshots, 8 positions unmanaged. Detection is NOT the gap — the canary alarmed correctly all four days. The gaps are (a) a repeating alarm reads identically to a first-day alarm, (b) GH Actions `conclusion` is always `success` (canary exits 0 by design), and (c) a deliberate pause and a catastrophic outage are byte-identical to monitoring. Fix = escalate on consecutive-day staleness AND/OR an explicit declared-pause switch. | escalation `[B-code]` · root cause (why n8n deactivated) `[B-spec]` | 2026-08-02: last execution of ANY stocks workflow = `2026-07-27T19:50Z`, all `success`, zero error executions — schedules simply stopped firing. The two legacy `[Markets]` workflows went inactive at the same moment and the only still-active workflow (Tech News) errored 08-01 → points at an instance-level event (Railway restart / plan limit), not a per-workflow pause. Mitigated only by 8/8 broker-side GTC trailing stops, which are independent of n8n. Canary alarmed 07-28/30/31 with `No orchestrator session today` — unactioned. **First incident in this fund's history where detection was perfect and nothing happened anyway** — the bottleneck has moved from monitoring to response. Deliberately NOT coded this week: the correct design (escalate vs suppress) depends on whether the pause was intentional, and building the wrong one either causes alarm fatigue or mutes the one layer that works. Both branches spec'd in the 2026-08-02 report §3. | **High** | Low | Low | `proposed` (needs one operator answer: was it deliberate?) | 2026-08-02 |
| 20 | **`pattern_performance` NOT NULL abort — the 07-19 remediation broke the 07-19 fix** — `pattern_type` is `NOT NULL`; the DELL row hand-reconstructed per the 2026-07-19 runbook §3 omitted `entry_pattern`; the recompute is a single `INSERT..SELECT..GROUP BY entry_pattern`, so that one NULL group aborts the WHOLE statement. `Update Pattern Performance` has `continueOnFail:true` → swallowed, execution reports success, rest of the post-mortem chain completes, canary green. Root fix = add `AND entry_pattern IS NOT NULL` to the node's WHERE (inline n8n node → manual) + backfill DELL. Detection coded as PR #30. | root fix `[B-spec]` · detection `[B-code]` | 2026-08-02: `updated_at` frozen at 2026-07-21 05:29 across ALL rows despite 6 real closes since (PLTR, AMAT, CHKP, NXPI, VRT, plus AXP/XOM). Recompute-from-source shows COUNTER_TREND (4 trades) and CAPITULATION (1) missing entirely, MOMENTUM 4-vs-5, PULLBACK 1-vs-2. **4th instance of the swallowed-write class** (06-03 dropped column → cash deadlock · 06-08 watchlist CHECK · 07-19 pattern CHECK · now pattern NOT NULL) and the **2nd break of this same table in 3 weeks, by a different mechanism**. Lesson recorded: remediation is itself a write, and writes break invariants — data-correction runbooks must state the full column list and end with a verification query. | High | Low | Low | **detection `coded PR#30`; root fix in 2026-08-02 runbook §2** | 2026-08-02 |
| 21 | **Orchestrator blind to earnings at ENTRY** — `02` computes `earningsAtRisk` only for tickers ALREADY HELD and only ≤7 days, so at entry-decision time the orchestrator sees no calendar data for candidates — only the specialist's free-text `earnings_risk` field. The prompt's own entry rule (§7 "earnings ≤2 days → reduce one tier"; decision-step 7) has therefore been unenforceable by construction since written. Fix = render the nearest future earnings date per candidate in §8 TECHNICALS + §9 guidance reserving `exit_reason: earnings_risk` for risk NOT knowable at entry. | `[B-code]` | 2026-08-02: **all 7 `earnings_risk` exits ever** were for dates already in `earnings_calendar` days-to-weeks BEFORE entry (WFC 14d before, NOC 17d, XOM 13d, AXP 7d, NXPI 7d, VRT 19d, CHKP 16d), and it is now **6 of the last 8 closes** — net −$213.90, avg hold 10.8d vs a 2–6 week mandate. The 2026-07-19 audit read this as a judgment failure and spec'd a prompt fix; that was only half right — the data was never there. Deliberately shipped as information, not a new `08` block: holding through earnings is legitimate (XOM +$250, AXP +$96), the failure is not *deciding* at entry. | **High** | Low | Low | **`coded PR#31`** (UNVERIFIED — no backtest) | 2026-08-02 |
| 22 | **Re-entering just-exited positions at worse prices** — the orchestrator closes a position and re-opens the same ticker days later around the same or a worse price, paying two sets of costs and re-setting the hold clock. Proposed rule: do not re-open a ticker closed within the last 10 trading days unless price has moved materially in your favour since the exit. | `[B-spec]` (judgment) | 2026-08-02: **AMAT** exited `position_aging` @$563.84 on 07-15 (−$99), re-bought @$563.31 on 07-21, stopped out @$513.02 on 07-27 → **−$402**. **VRT** stopped out @$310.92 on 07-01, re-bought @$303.55 on 07-21, exited `earnings_risk` 07-27 → −$204. **PANW** stopped out @$333.23 on 07-08 for a **+25% win**, re-bought @$342.15 on 07-22 (above its own exit), currently −2.9%. 3 instances in one fortnight. Folded into runbook §4 with the exit-vocabulary loophole. | Medium | Low | Low | `proposed` | 2026-08-02 |
| 15 | **Legacy-pattern veto resurrection** — post-Phase-1, legacy `pattern_performance` rows (TREND/BIAS/...) are frozen forever (new trades store price-based labels), but `06` still rendered `← NEGATIVE EV — DO NOT TRADE` on them whenever dominance share < 60%. TREND sits at exactly 15/25 = 60.0%, so the next non-TREND close (OXY is BIAS-labeled) would flip the flag back on **permanently** — the frozen row can never update — resurrecting the fund-wide entry veto that caused the 06-24→07-02 98%-cash deadlock. Fix: legacy rows render as `LEGACY LABEL — historical context only, NOT an entry gate`; veto/dominance/LOW-EV flags scoped to active price-based rows; dominance share computed within the active record. | `[A]` | 2026-07-05: found while verifying the 07-03 recompute — the guard's `share >= 0.6` sits exactly at threshold with a BIAS-labeled position open. The orchestrator quoted the table flag verbatim every session of the deadlock; the flag is the operative signal, and §7b prose already said legacy rows are "history, not guidance". | High | Low | Low | **`coded PR#21` (2026-07-05)** | 2026-07-05 |

## R&D Initiatives (Lever 7b — research agenda)

Forward-looking ways to *add edge* (not just fix breakage). The audit advances each maturity grade week-over-week using fresh evidence — it does NOT re-brainstorm. An initiative reaching `evidence-backed` AND isolated/code-only is eligible to be promoted to a coded `[B-code]` PR; strategy ideas stay `[B-spec]` until the backtest harness exists. Seeded 2026-06-11 from the operator design discussion.

| Idea | Category | Tier | Maturity | Why it could add edge / evidence so far | First raised |
|------|----------|------|----------|------------------------------------------|--------------|
| **Backtest / test harness** — replay a proposed rule against the ~350 daily bars already in-pipeline before it goes live. | meta-infra | `[B-code]` | **`evidence-backed` → v0 BUILT (PR #22, 2026-07-05)** | **Keystone** — it's what makes strategy ideas *verifiable*, the prerequisite to ever coding them and to safely expanding audit autonomy. **2026-07-03: reinforced a 4th straight week** — PR #18 ships UNVERIFIED, and both the taxonomy redesign and the regime governor are blocked on replay capability. **2026-07-05 — PROMOTED + BUILT:** promotion evidence = PR #18 was inert on first live contact (a replay would have caught 58.6% < 60%) and Phase 1 shipped 5 unverified thresholds. `automation/backtest.mjs` (read-only, zero blast radius) replays the 07 taxonomy, the 08 ±5% gate, and the 02 regime classifier with exact live math; first run produced 3 decision-relevant findings (short gate validated · long gate contradicted-in-period · regime label contrarian at 21d). Next: extend from signal analysis toward rule-replay with stops/sizing. **2026-07-19: verified NOT affected by the bars-truncation bug** (fetches in batches of 10 at limit=10000) — the 07-05 findings stand. **2026-08-02: no movement** — no new replay run this fortnight. Its two standing customers (regime thresholds, the short/pairs question now sharpened by the 5/9-vs-9/31 split) both remain blocked on rule-replay with stops and sizing. Still the keystone. | 2026-06-11 |
| **Fill-based trade recording** — record `trades.entry_price` (and `position_metadata.entry_price`) from the Alpaca order's `filled_avg_price` in the execution branch (post-`Restore Trade Context`), instead of the bars-derived `priceMap` estimate. | meta-infra | `[B-spec]` (n8n wiring) | `promising` | NEW 2026-07-19: the bars-truncation incident showed recorded prices can diverge 30%+ from real fills (VST $210.40 vs $152.13), and even healthy bars carry 1–3% overnight-gap drift into every P&L/attribution figure (NXPI recorded $283.81 vs $292.57 filled). Recording the fill makes the corruption class impossible by construction; PR #25's reconciliation then becomes a pure regression check. **2026-08-02 — new evidence, different failure mode:** CORZ was written to `trades` (07-23, 127 sh, $3,000) from an Alpaca order that was **`canceled` and never filled** (`filled_avg_price: null`). So the table records *intent*, not execution — a superset of the stale-price problem. Cleaned to `never_executed` 07-24, zero impact, but it shows the write path has no fill precondition at all. Holds `promising` only because it still needs n8n wiring. | 2026-07-19 |
| **Regime-conditioned long-side extension gate** — keep 08's short-side −5% block unconditional (validated); make the long-side +5% block regime-aware (block in BEAR/NEUTRAL, allow in computed BULL at one size tier lower). | risk/guardrail | `[B-spec]` | `promising` | NEW 2026-07-05, from harness v0: long forward-21d returns are **U-shaped in extension** — the gate confines entries to the weakest middle (±2% ≈ +2.4% avg) while EXTENDED longs averaged +6.39% (64% win, +4.28 alpha)… but flip to −1.10% (41% win) in computed BEAR regime. One momentum-heavy period, no stops modeled — a judgment change to a 2-day-old operator-built gate, so spec-only; decide after a few weeks of live Phase-1 entries. | 2026-07-05 |
| **Earnings as opportunity (PEAD)** — trade post-earnings drift (beat + gap → 2–6 wk drift) instead of using `earnings_calendar` only as a "don't enter ≤3d" risk gate. | alpha strategy | `[B-spec]` | `speculative` | Strongest documented swing edge; data already present, only its downside is used today. Fits the 2–6 wk horizon exactly. Needs harness to validate in our universe. | 2026-06-11 |
| **Cross-sector rotation / macro-regime + deployment-TIMING input to orchestrator** — `Compute Sector Rotation` node: RS of each of the 10 sector ETFs vs SPY over 1/4/12 wks + a simple risk-on/off read (SPY vs 20/50d MA), fed to the orchestrator, PLUS a prompt rule: "when SPY has broken below its short-term trend (falling 20d MA), do not add new longs — hold/trim/favor shorts." | alpha strategy | `[B-spec]` | `promising` (re-aimed) | Architectural gap — orchestrator has no quantified risk-on/off input. **2026-06-17→06-21:** removing the EV brake didn't unfreeze the book; concluded the missing piece was a *positive* deployment governor. **2026-06-28 — RE-AIMED:** the cash drag broke on its own (fund deployed 06-22, no code change). The job is no longer "force deployment" but "**time deployment**" — the orchestrator top-ticked, deploying 7 TREND longs at 09:32 directly into a 3-day SPY decline (every long lost). **2026-07-03:** consistent evidence (no regime counterweight to the pattern-EV veto in a bull tape → 98% cash) but not new mechanism evidence; grade holds. **Escalation trigger defined: if deployment refusal re-routes qualitatively after PR #18 merges, this becomes the required architectural fix** — the pattern of five audits (every brake channel routes to non-deployment) says annotation fixes are running out. **2026-07-03 — SUBSTANTIALLY BUILT (operator, PR #20 commit 5):** no new n8n node needed — `02` already had all bars; it now computes BULL/NEUTRAL/BEAR deterministically (SPY vs 20d/50d SMA + sector-ETF breadth) and `06` prints it in the session header with a net exposure target, replacing the self-assessed regime read. The prompt frames sub-band cash in a computed BULL regime as an active bet requiring gate-by-gate justification. Remaining: backtest validation of the regime thresholds + judge live whether it actually times deployment better. **2026-07-05 — CAUTION FLAG from harness v0:** the computed regime label is *contrarian* at 21d forward SPY in the 2025-01→2026-07 sample (BEAR → +4.51% avg fwd, best of the three; V-recovery artifact, n=50 overlapping sessions). The label is fine as *information* (already live); hardening it into stronger deployment mechanics should wait for live evidence + deeper replay. Grade holds `promising`. **2026-07-19 — input-feed hazard:** the computed regime reads SPY + sector-ETF breadth from the `Fetch Bars SPY` node (limit=4400, 11 symbols × ~385 bars = 4,235 needed today) — it exhausts ~early August and the regime label starts computing off stale ETF series unless the limit is bumped (structural #16 runbook §1). **2026-08-02 — HAZARD CLEARED:** the operator's 07-21 bump to `limit=10000` landed before the SPY/ETF node exhausted; canary replay confirms all 11 ETF series current. The regime input is clean. No new evidence on whether the label *times deployment* better, because the fund has been off since 07-27 — grade holds `promising`. | 2026-06-11 |
| **Pattern-label monoculture / TREND −EV concentration** (🔝 top bet 2026-07-03) — almost every entry tags `entry_pattern = TREND`, so pattern-EV feedback is a constant (the fund's overall record), not a discriminator. Two halves: (a) **veto guard** — stop the ≥5-trade negative-EV rule from acting as a fund-wide entry veto when one pattern dominates; (b) **taxonomy enrichment** — make the classifier discriminative again (TREND-early vs TREND-extended by distance from 20d MA; pullback vs breakout; or pivot feedback to niche × side × regime). | signal quality | (a) `[A]` · (b) `[B-spec]` | **`evidence-backed`** (promoted from `speculative` 2026-07-03) | **2026-06-28 (new):** all 06-22/06-23 entries tagged TREND; n=12, EV −1.32%. **2026-07-03 — DEADLOCK CONFIRMED LIVE:** TREND = 17/25 all-time closed trades (68%), EV −1.50% past the ≥5 floor → `06` rendered `← NEGATIVE EV — DO NOT TRADE` every session → **zero entries 7 straight trading days, ~98% cash** in a self-described bull tape; orchestrator quoted the veto verbatim as its reason each session and closed BAC "to align with the feedback system." Self-sealing: no new TREND trades → EV can never update. Half (a) **coded as PR #18** (dominance ≥60% → annotation, not veto; UNVERIFIED banner). **2026-07-03 post-merge: PR #18 found INERT on its first live session** — it computed TREND share as 17/29 = 58.6% (< 60%) because `pattern_performance` counted 4 phantom `never_executed` rows; fixed same day: `Update Pattern Performance` SQL now excludes them (inline n8n edit + one-time recompute → TREND 15/25 = 60.0%, guard fires; stale REVERSAL row deleted). Half (b) **built the same day, beyond the original spec**: PR #20 commit 4 replaces the taxonomy at the source — every new entry is classified by measurable price conditions (PULLBACK/BREAKOUT/MOMENTUM/COUNTER_TREND/EXTENDED/CAPITULATION from 20d-SMA extension, 50d trend side, volume) in `07` + one inline-node edit. New labels accumulate their own EV rows; legacy rows are history. UNVERIFIED (no backtest); first labeled entry pending. **2026-07-05 — resurrection path closed (PR #21):** legacy rows are frozen post-Phase-1, and with TREND at exactly 15/25 = 60.0% the next non-TREND close would have permanently re-armed the fund-wide veto; `06` now renders legacy rows as history-not-gate and scopes the veto to active price-based rows (see structural #15). Status → **watch**: first price-based EV rows accumulate from Mon 07-06. **2026-07-19 — BLOCKED: zero new-taxonomy rows collected** — the `pattern_performance` CHECK constraint rejects every new label (structural #17); 4 closes' worth of EV data lost (recoverable — the recompute back-fills from `trades` after the ALTER). PR #21 is meanwhile the only thing keeping the legacy rows from acting as a gate. **2026-08-02 — STILL BLOCKED, new mechanism.** The CHECK constraint was fixed 07-21 and a one-time recompute ran, but the table re-froze immediately: a NULL `entry_pattern` on the hand-inserted DELL row aborts the whole `INSERT..SELECT` against a `NOT NULL` column (structural #20). Three weeks after Phase 1 went live the new taxonomy still has **zero** accumulated EV rows — COUNTER_TREND (4 closed trades) and CAPITULATION (1) have no row at all. Detection coded as PR #30; 1-line root fix in the 2026-08-02 runbook §2. | 2026-06-28 |
| **Alarm escalation + declared-pause switch** — make a *repeating* canary alarm escalate (consecutive-day counter in the message's first line; optionally let the daily-canary Action exit non-zero once staleness exceeds one trading day so GitHub's own failure notifications fire), and/or add an explicit `PAUSED_UNTIL` switch the canary honours so a deliberate pause is silent while an undeclared stall is loud. | meta-infra | `[B-code]` | **`evidence-backed`** (new, enters at top grade) | NEW 2026-08-02, and the strongest evidence any initiative has ever entered on: the fund was switched off for **4 trading days** (07-28→07-31, all `[Stocks]` workflows `active=false`) and the canary alarmed **correctly, every single day**, with no response. This is the first incident here where detection was perfect and the outcome was still a 4-day outage — every prior Lever-1 failure was a monitoring gap. The bottleneck has moved from detection to response, which is a different engineering problem: day-1 and day-4 alarms are visually identical on a phone, GH Actions reports `conclusion: success` throughout (canary exits 0 by design — see PR #28), and a deliberate pause is byte-identical to a catastrophic outage. **Deliberately not coded this week:** escalate-vs-suppress depends on whether the shutdown was intentional, and building the wrong branch either causes alarm fatigue or mutes the only layer currently working. One operator answer settles it. Both branches spec'd in the 2026-08-02 report §3. | 2026-08-02 |
| **Analyst rating *changes* (not levels)** — surface MoM deltas from Finnhub recommendation-trends, not the static buy/hold/sell counts. | signal (free) | `[B-code]?` | `speculative` | A fresh upgrade/downgrade is a dated catalyst; a static "75% buy" is wallpaper. Already calling the endpoint. Likely isolated/code-only → candidate `[B-code]` once evidenced. | 2026-06-11 |
| **Macro RSS feed → orchestrator** — one market-wide feed (Fed/CPI/jobs/broad-market) routed to the orchestrator, not specialists. | signal (free) | `[B-spec]` | `speculative` | Pairs with sector-rotation; no dedicated macro input exists. Needs an n8n node wire-up → `[B-spec]`. | 2026-06-11 |
| **Formalized pairs-spread tracking** — track the long-leader/short-laggard spread vs its own history; enter stretched, exit on convergence. | alpha strategy | `[B-spec]` | `promising` (promoted 2026-07-05) | Makes the existing informal leader/laggard mandate quantitative → genuine market-neutral alpha. **2026-07-05 evidence (harness v0):** every outright short entry profile carried negative avg forward return in this universe/period (best bucket still −1.9%; post-collapse shorts −5.9%) — consistent with the realized short book (1/5, −$532). Outright directional shorts are structurally hard here; the one durable short winner (OXY, +15%) is effectively a sector-pair expression. Pairs are the defensible short vehicle. Still blocked on harness rule-replay for validation. **2026-07-19:** both fortnight shorts came from orchestrator judgment against a 76.8%-bullish / 1.6%-bearish specialist feed (the leader/laggard mandate is dead in output); SMCI short banked +$199 (broker-true), CHKP short open. **2026-08-02 — STRENGTHENED:** across all 40 real closed trades the short book is now the *better* book — **short 5/9 (56%), −$86** vs **long 9/31 (29%), −$1,315** — and it is produced entirely by orchestrator judgment against a specialist feed that emitted **zero** BEARISH signals in 210 (Lever 3). That directly contradicts the 07-05 harness read that outright shorts are structurally unprofitable here; small sample (9), but it is now the clearest edge signal in the record. Still blocked on harness rule-replay before coding. Holds `promising`, close to `evidence-backed`. | 2026-06-11 |
| **Cash-deployment discipline + cold-start feedback weighting** (RESOLVED as a deployment blocker) — fix the feedback that throttles deployment (DONE) AND add a positive deployment mandate. | risk/strategy | `[A]`+`[B-spec]` | **`worked` (drag resolved 06-28)** | 2026-06-12→06-21: diagnosed + EV-floor fixes (PRs #7/#8) shipped; drag initially survived qualitatively. **2026-06-28 — DRAG BROKE:** fund deployed ~$14k on 06-22 with no further code change; cash is no longer the binding constraint. The EV-floor PRs worked at their layer. **Residual concern folded into the macro-regime/timing row above** — the problem is now deployment *timing*, not deployment *volume*. Retire this row's "won't deploy" framing. | 2026-06-11 |
| **Correlation-aware / vol-aware short sizing** — turn the 0.70 advisory penalty into a portfolio-level exposure cap; size shorts for squeeze risk. | risk/guardrail | `[B-spec]` | `promising` | FTNT/PANW/DDOG all >0.70 correlated longs (persists 2026-06-12). **New 06-12 evidence on the short side:** SQM short squeezed −8.64% (COPX −11.74% but SQM rose) — first concrete short-side guardrail event; one data point, watching for a pattern. Same as structural #5. | 2026-06-11 |
| **Finnhub news-sentiment / insider transactions** — free-tier confirming signals. | signal (free) | `[B-spec]` | `speculative` | Cheap conviction overlay; check against the 60 req/min budget before adding per-ticker calls. | 2026-06-11 |
| *(⛔ named-but-never-coded)* options flow · short-interest feed · FMP premium price targets | signal (paid) | `[B-spec]` ⛔ | `speculative` | Real signals but paid — outside §0.5 envelope. Surface with cost, never code. | 2026-06-11 |

---

### Notes for the auditor
- **Dedup:** before adding an idea, check it isn't already a row above (even reworded). If it is, update its evidence/maturity/rank instead of adding a duplicate.
- **Advance, don't re-brainstorm:** each week move maturity grades with fresh evidence (`speculative → promising → evidence-backed`) or kill an idea. Honest "still speculative, no new evidence" is fine — do NOT manufacture novelty.
- **Promote to code carefully:** an initiative becomes a `[B-code]` PR only when `evidence-backed` AND isolated/code-only (no n8n wiring, low blast radius). Strategy stays `[B-spec]` until the backtest harness exists.
- **Close the loop:** when an item is coded into a PR, record `coded PR#<n>`; after merge, the next audits judge whether the targeted metric improved and set `worked` / `didn't`.
- **Tag honestly:** Risk = risk-to-stability of the live fund if implemented. High-impact + low-risk rises to the top.
